const producto = {
    nombre: "monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}



const { nombre } = producto;
const { precio } = producto;
console.log(producto.nombre);
console.log(precio);